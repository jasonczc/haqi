{
    "name": "@hapi/daemon",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun --watch src/index.ts",
        "build": "bun scripts/build.ts",
        "typecheck": "tsc --noEmit",
        "test": "bun test"
    },
    "exports": {
        "./types": "./src/types.ts"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "zod": "^3.23.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.0.0"
    }
}
